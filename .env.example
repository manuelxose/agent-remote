# Copy to .env and replace placeholders. Never commit .env or WhatsApp auth data.
WHATSAPP_AUTH_PATH=./data/whatsapp-auth
WHATSAPP_ALLOWED_USERS=your-number@s.whatsapp.net
WHATSAPP_ALLOWED_CHATS=
WHATSAPP_ALLOW_SELF_MESSAGES=true
# Native quoted-reply context is bounded and expires automatically.
WHATSAPP_REPLY_CONTEXT_TTL_MS=120000
WHATSAPP_REPLY_CONTEXT_MAX_ENTRIES=256
AGENT_REMOTE_ROUTES_PATH=./config/routes.json
AGENT_REMOTE_WORKSPACE_ROOTS=../your-project
AGENT_REMOTE_DEFAULT_WORKSPACE=../your-project
AGENT_REMOTE_WORKSPACE_ALIASES={"agent-remote":"../your-project"}
AGENT_REMOTE_SESSION_PATH=./data/developer-agent-sessions.json
AGENT_REMOTE_CONTROL_PLANE_PATH=./data/control-plane.json
AGENT_REMOTE_TIMEOUT_MS=120000
AGENT_REMOTE_MAX_OUTPUT_BYTES=65536
AGENT_REMOTE_MAX_QUEUE_DEPTH=8
AGENT_REMOTE_RATE_LIMIT_PER_MINUTE=60
# WhatsApp sends final replies by default; one delayed progress reply is optional.
AGENT_REMOTE_PROGRESS_AFTER_MS=0
AGENT_REMOTE_PROGRESS_TEXT=Sigo trabajando…
AGENT_REMOTE_STREAM_MAX_MESSAGES=2
AGENT_REMOTE_LOCK_PATH=./data/agent-remote.lock
# Explicit underlying provider models for the default sonnet and luna selections.
# AGENT_REMOTE_CLAUDE_MODEL=<configured-claude-model-id>
# AGENT_REMOTE_CODEX_MODEL=<configured-codex-model-id>
# Optional JSON alias maps for /model selection.
# AGENT_REMOTE_CLAUDE_MODELS={"fast":"<fast-claude-model-id>","quality":"<quality-claude-model-id>"}
# AGENT_REMOTE_CODEX_MODELS={"fast":"codex-mini-latest","quality":"<configured-codex-model-id>"}
# Optional role overrides; allowlisted WhatsApp users default to owner.
# AGENT_REMOTE_OWNER_IDS=your-number@s.whatsapp.net
# AGENT_REMOTE_OPERATOR_IDS=
# AGENT_REMOTE_VIEWER_IDS=
# Optional absolute executable overrides, useful when a CLI is installed on Windows but the gateway runs in WSL.
# AGENT_REMOTE_CLAUDE_EXECUTABLE=/mnt/c/Users/your-user/.local/bin/claude.exe
# AGENT_REMOTE_CODEX_EXECUTABLE=/mnt/c/Users/your-user/.local/bin/codex.exe
# AGENT_REMOTE_COPILOT_EXECUTABLE=/mnt/c/Users/your-user/.local/bin/copilot.exe
