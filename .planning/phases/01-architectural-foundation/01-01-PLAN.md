# Phase 1 Plan: Architectural foundation

**Goal:** Deliver a minimal compiling TypeScript foundation for channel-neutral message routing into two explicitly separated runtime trust zones.

**Requirements:** CORE-01, CORE-02, CORE-03, ROUTE-01, ROUTE-02, EVENT-01, SEC-01, SEC-02, SEC-03, ADAPTER-01, ADAPTER-02, DOC-01, TEST-01.

**Execution:** Implement tasks in `docs/superpowers/plans/2026-09-10-agent-remote-foundation.md` sequentially because each package contract feeds the next. Use Node built-ins and npm workspaces only.

**Verification:** `npm test`, `npm run build`, and `node --test test/boundaries.test.ts` must exit 0. Static boundary assertions and runtime capability tests are required evidence.
