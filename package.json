{
  "name": "agent-remote",
  "private": true,
  "type": "module",
  "dependencies": {
    "@whiskeysockets/baileys": "^7.0.0-rc14",
    "qrcode-terminal": "^0.12.0"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "npm run build && node --test --experimental-strip-types test/*.test.ts",
    "start": "npm run build && node dist/apps/gateway/src/main.js",
    "doctor": "npm run build && node dist/apps/gateway/src/main.js doctor"
  }
}
